<h1>Update Owner</h1>

<form action="/owners/<%= @owner.id %>" method="POST">
  <input id="hidden" type="hidden" name="_method" value="PATCH">
  <label>Name:</label>

  <br>

  <input type="text" name="owner[name]" value="<%= @owner.name %>">

  <br>

  <label>Update existing pet(s):</label><br>
  <% @pets.each do |pet| %>
    <% @owner.pets.include?(pet) ? (exist = true) : (exist = false) %>
    <input type="checkbox" name="owner[pet_ids][]" id="<%= pet.id %>" checked="<%= exist %>" value="<%= pet.id %>"><%= pet.name %></input><br>
  <% end %>

  <br>

  and/or, create a new pet:
    <br>
    <label>name:</label>
      <input  type="text" name="pet[name]"></input>
    <br>

  <input type="submit" value="Update Owner">
</form>
