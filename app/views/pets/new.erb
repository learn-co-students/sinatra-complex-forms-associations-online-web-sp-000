<h1>Create a new Pet</h1>

#The form to create a new pet should allow a user to select from the list of available owners and/or create a new owner,

<form action="/pets" method="POST">
  <label>Pet Name:</label>
  <br>
  <input type="text" name="pet[name]">
 
  <br>
  <p>Select an existing owner or create a new one below.</p>
 
 
  <h3>Existing Owners:</h3>
  <% @owners.each do |owner| %>
    <input type="checkbox" name="pet[owner_ids][]" id="<%= owner.id %>" value="<%= owner.id %>"><%= owner.name %></input><br>
  <% end %>
  <br>
 
  <h3>Or Add A New Owner:</h3>
  <label>Owner Name: </label>
  <br>
  <input type="text" name="owner[name]" id="owner_name"></input>
  <br><br>
 
  <input type="submit" value="Create Pet">
</form>

