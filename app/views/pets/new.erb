<h1>Create a new Pet</h1>
<form action = '/pets' method = "POST">
<label>Name: </label>
<br>
<input type = "text" name = "pet[name]" id = "pet_name"><br>
<h3>Owner: </h3>
<% @owners.each do |owner| %>
    <input type = "radio" name = "pet[owner_id]" value = "<%= owner.id %>" id = "<%= owner.id %>" ><label><%= owner.name %></label><br>
    <% end %>
<h2>Create New Owner</h3>
<label>Name: </label>
<input type = "text" name = "owner[name]" id = "owner_name"><br>

<input type = "submit" id = "Create Pet" value = "Create Pet">
    </form>
