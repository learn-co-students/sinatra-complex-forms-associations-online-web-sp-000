<h1>Create a New Pet</h1>

<form action="/pets" method="post">
  <label>Pet Name: <input type="text" name="pet[name]" id="pet_name"></label><br>

  <p>Pick one owner for the pet:</p>
  <% @owners.each do |owner|%>
  <input type="radio" name="pet[owner_id]" id="<%= owner.id %>" value="<%= owner.id %>"><%= owner.name%></input>
  <br>
  <% end %>


  <p>Or create a owner for it:</p>
  <input type="text" name="owner[name]" id="owner_name">
  <br>


  <input type="submit" value="Create Pet">
</form>
